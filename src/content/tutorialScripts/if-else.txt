[img: /assets/tutorials/language-fundamentals/if-else.svg]
If and else let the game choose between two paths.
The condition after if is checked first. If it is true, the commands inside the if block run.
If the condition is false, SceneMax skips that block and runs the else block instead.
In this example, the player gets the bonus message only when score equals 10.
Use if and else for doors, rewards, health checks, game-over rules, and any moment where the game must make a decision.
