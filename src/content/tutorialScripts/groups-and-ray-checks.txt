Groups & Ray Checks explains how the game reacts to player input or scene events.
Use this when the timing depends on the player or the scene. The command runs because something happened.
In a fighting game, language fundamentals track health, score, current action, input state, and whether a hit is allowed.
The sample begins with D belongs to the enemies group. In plain language, it shows the command or data shape used by this lesson.
The key idea is timing: the command belongs to a moment, not just to a line number.
